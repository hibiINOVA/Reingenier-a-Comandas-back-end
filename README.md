Después de clonar tu proyecto

composer install

Si tienes Xampp, haz las pruebas desde la ruta de tu htdocs o crea un contenedor de docker con el archivo: docker-compose.yml

De lo contrario ejecuta el comando php -S localhost:8000 -t public public/index.php para correr el proyecto# Reingenier-a-Comandas-back-end
